{
  "name": "nw-builds",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "start": "node ./server-build/Server.js",
    "start-dev": "nodemon ./src/Server.ts",
    "build-backend": " tsc && copyfiles server-build/",
    "build-frontend": "cd frontend && npm i && npm i -g react-scripts && npm run build",
    "build": "npm run clean && npm run build-backend && echo Finished building backend! && npm run build-frontend && echo Finished building frontend!",
    "clean": "rimraf server-build/ && rimraf ./front-end/build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "bcrypt": "^5.0.1",
    "beautiful-react-hooks": "^1.0.2",
    "copyfiles": "^2.4.1",
    "express": "^4.17.1",
    "express-validator": "^6.13.0",
    "jsonwebtoken": "^8.5.1",
    "nodemon": "^2.0.13",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3",
    "winston": "^3.3.3"
  }
}
